# config
Assorted software configurations and settings

windows
* Install Python
* Install WSL (wsl --install)
* Install Windows terminal
* Install VSCode (user)
* Install docker
* Install font
* Run setup script